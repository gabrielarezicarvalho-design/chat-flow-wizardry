-- Adicionar provider 'asaas' na constraint se existir, ou permitir qualquer provider
-- Primeiro vamos verificar se podemos inserir 'asaas' como provider

-- Não precisamos alterar estrutura, a tabela ai_provider_keys já aceita qualquer provider string
-- Vamos apenas garantir que podemos usar 'asaas' como provider